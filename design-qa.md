# Design QA

## Source Truth

- Reference image: `/var/folders/_j/tgwn359j6lv2m4lncms30rnw0000gn/T/TemporaryItems/NSIRD_screencaptureui_snSApi/Screenshot 2026-07-24 at 8.17.46 PM.png`
- Secondary visual reference: `https://www.douyin.com/user/MS4wLjABAAAAs19HZ0ecHKqv0HuopcsXbgx1-pPx9b5sDh08LH9AAs0boFwZ5Gpixyub48yU8FfG?from_tab_name=main`
- Implementation: `http://127.0.0.1:4177/#double-slit`
- Source viewport: 2354 x 1482 px
- Extended source set: `/Users/maxwang/Desktop/Screenshot 2026-07-24 at 9.09.50 PM.png` through `/Users/maxwang/Desktop/Screenshot 2026-07-24 at 9.14.35 PM.png`

## Comparison

- Full desktop comparison used the source image and the implementation capture together at 2354 x 1482 px.
- The finished lab preserves the reference's dark, high-density scientific-instrument character while retaining this project's Taiwan curriculum navigation, terminology, and original physical model.
- The double-slit scene now visibly communicates an optical bench: laser housing, support feet, rail graduations, double slit, interference wavefronts, detector screen, bright-stripe preview, photon accumulation, order marks, and a measurement bracket.
- The shared simulation shell is applied by `PhysicsLab.ui.layout()` and `PhysicsLab.draw.bg()`, so all 200 experiments receive a consistent visual stage, parameter deck, real-time readout deck, instrument grid, and labelled chart frame.
- The secondary reference informed the cleaner apparatus silhouettes, restrained technical labels, and clear separation of the physical model from its measurement data; no short-video layout or third-party artwork was copied.
- The extended source set informed the new experiment-workbench hierarchy: clear instrument title, separated visual/control/data regions, structured procedure state, and data-first control tools. The implementation keeps the project’s original dark lab system rather than copying third-party screen composition or artwork.

## Focused QA

- Desktop: no stretched visual panel or unused in-panel vertical space after independent panel sizing.
- Mobile: checked at 390 x 844 px; document scroll width equals viewport width, the canvas is 304 x 164 px, and all three lab panels are present without overlap.
- Interaction: the unique `波長 λ (nm) 增加` control changed the value from 600 to 610 and redrew the live experiment.
- Runtime: browser console reported no errors on the refined double-slit page.
- Subject-stage sampling: checked mechanics, thermal, circuit, and magnetism screens. Each receives the appropriate stage name, calibration rail, colour treatment, and family-specific instrument chrome.
- Shared workbench controls now provide functioning experiment guidance, step-by-step procedure state, focus view, full-screen view, reading export, and primary-canvas capture for all registered experiments.
- Access gate is intentionally scoped as a page-lifetime barrier for a static GitHub Pages site; reloading or opening a new tab requires the password again. It is not represented as server-side access control.
- Curriculum registry: 12 modules, 200 curriculum IDs, and 200 registered simulations; duplicate, missing, and extra-ID checks are all empty. The app now repeats this audit at startup and exposes the result as `window.PhysicsLabAudit` for browser QA.
- Expanded-lab check: `核反應與質能轉換` renders its dedicated micro-observation scene, two live controls, calculated readout, graph, formula, and learning points. One keyboard increment of mass defect changed the model output from `664.59` to `709.37`.
- Responsive recheck: at 390 x 844 px, document scroll width equals the viewport width and the six shared workbench commands wrap into a clear two-column control grid without overlap.
- Runtime recheck: the expanded lab browser console reported no warnings or errors.
- Comprehensive extension check: the 44 fourth-batch labs were each opened through the site's own search and route flow. All 44 rendered their named experiment page, control deck, live readouts, relationship chart, formula, and learning points without a browser runtime error.
- Representative visual check: the `輻射平衡與溫室效應` stage renders a dedicated greenhouse chamber with incoming and outgoing radiation arrows, rather than a reused static image. Mechanics, optics, circuits, and modern physics were likewise sampled with `水火箭與反作用力`, `光纖傳輸與彎曲損耗`, `RLC 串聯共振與相位`, and `陰極射線管與電子比電荷`.

## History

- Added the common experiment-bench hierarchy and responsive layout.
- Added shared canvas calibration grid, corner marks, chart ticks, and denser readout surfaces.
- Elevated double-slit to a flagship optical-bench simulation.
- Added profile-driven stages for all 12 curriculum modules: motion / force / momentum / energy / oscillation use a mechanics rail, orbital topics use an observation field, thermal topics use a bench, waves use a wave guide, optics use an optical rail, circuits use terminals, magnetism uses field coils, and modern physics uses a detector field.
- Added the common guided-workbench command layer and per-page access gate across the complete curriculum.
- Extended the curriculum from the junior-high natural-science bridge through senior-high electives with 44 themed simulations: sensing, simple machines, fluid and thermal phenomena, sound, imaging, household circuits, electromagnetism, and modern-physics instruments.

## 2026-07 淺色主題與可用性修正

### 問題根因

- 淺色主題僅覆寫十餘個變數，其餘表面色寫死為 `rgba(255,255,255,α)`，在白底上等於白疊白，分隔線、面板頭、格線與襯底面全部消失。
- 84 個實驗檔中有 247 處寫死的白色系繪圖色（`#fff`、`#e6edf3`、半透明白），深色台上清楚，淺色台上整片不可見。
- 模組識別色為 Material 300/400 亮色，當作文字放在白底上僅約 1.4:1。
- 列印樣式只改 `body` 的顏色，子元素仍吃深色主題變數，列印結果為白紙淺灰字。

### 對應處置

- 新增語意化表面色階（`--tint-1/2/3`、`--edge-hi`、`--grid-line`、`--scrim`、`--on-accent` 等），兩個主題各自定義。
- 在 `sim-core.js` 加入主題感知墨色層：以 12px 粗網格追蹤背景亮度，繪圖時依背景決定是否翻墨；純算術實作，量測為每影格額外約 0.13 ms。
- 新增 `--m-ink` 系列變數，淺色主題下把模組色壓深 45% 後再當文字色。
- 列印樣式改為整組覆寫變數。
- 授權頁補上主題同步與切換鈕；兩頁都加入樣式表載入前的主題預設腳本，消除深色閃爍。

### 驗證

- 對比檢查腳本：兩主題共 32 組前景／背景組合，加上 12 個模組色的文字用法，全部 ≥ 4.5:1（AA）。
- 墨色層單元測試 15 項全數通過，含「彩色圓點上的白字須保留白色」與「壓暗後色相偏移 < 6°」。
- 全站渲染測試：245 個模擬在深色與淺色主題下各建置並重繪兩次，零例外。
- 實驗檔中 65 種寫死顏色在淺色實驗台上的最差對比由 1.12 提升至 3.00。
- 邏輯測試 21 項全數通過（學習計劃切分、最小平方法、章節檢核觸發條件）。

## 2026-07 對照市面模擬器的功能升級

### 依據

主要依據 PhET Interactive Simulations 公開的《PhET Look and Feel — Underlying Ideas》
（設計準則來自實際的學生訪談），並參考 Physics Aviary 的隨機化實驗題設計。

### 對照後找出的落差與處置

| PhET 準則 | 本站原狀 | 處置 |
| --- | --- | --- |
| 「點擊拖曳是最自然的動作，看起來有用的東西學生都會想去拖」 | 245 個實驗全部只能用滑桿，畫面上沒有任何東西可以碰 | 新增疊圖工具層，可拖曳的碼錶與量角器對全部 245 個實驗生效 |
| 「為鼓勵量化探究應提供尺、碼錶等量測儀器」 | 完全沒有量測工具 | 碼錶量模擬時間（慢動作正確計入）；尺需 `cv.calibrate()` 才提供，已為 6 個有真實長度尺度的實驗校準 |
| 「學生不會自己找到播放／暫停鍵，但看到就會用」 | 只有部分實驗有播放鍵，且藏在指令列 | 獨立的時間控制列放在實驗台正上方，含單步與 0.25×～2× 慢動作 |
| 「模擬啟動時應該幾乎不動，才能鼓勵探索」 | 每個實驗一進場就自動播放 | 在 `loop()` 攔截建置期的 `start()`，改為停在第一格並以 wiggle-me 指向播放鍵 |
| 「play area 裡的文字是干擾」 | canvas 上畫了整句解說 | 4 句純解說移到畫面下方的說明列；圖表標題與座標約定屬圖說本身，保留 |
| 「控制面板的文字一到三個字最有效」 | 面板內有整段 note | 超過 34 字的 note 改為預設收合 |
| 市面標配的 Reset All | 沒有 | 控制項工廠統一登記初始值，一鍵還原全部參數、資料與計時 |
| Physics Aviary 的隨機化實驗題 | 全站皆為選擇題 | 8 個實驗新增隨機化計算題，種子由裝置＋日期決定，含相對容差判定與逐步解法 |

### 實作要點

- 全部透過 `layout()`、`loop()`、控制項工廠這三個共用入口攔截，245 個實驗檔零修改即生效。
- 工具層畫在獨立疊圖畫布上，不介入實驗自己的 `draw()`；未開啟工具時 `pointer-events: none`，完全不影響原本操作。
- 隨機題種子改用 splitmix32：原本的線性同餘產生器對相鄰種子的第一個輸出幾乎相同，量化後會出現「按了換一題卻沒換」。

### 驗證

- 工具層與時間控制互動測試 27 項全數通過（含「進場停在暫停」「重設還原滑桿」「碼錶在 0.25× 下只走 0.25 s」「未校準的實驗不提供尺」）。
- 隨機題目測試 18 項：每題以 400 個種子生成，並用題幹數字獨立重算驗證答案，最大相對誤差 0.59%（來自題幹的四捨五入）。
- 全站渲染測試：245 個模擬在深淺兩主題下各建置並重繪，零例外。
- 對比檢查、墨色層測試、既有邏輯測試全數維持通過。

## 2026-07 學習鷹架：讓學生知道「要看什麼、怎樣算做完」

### 問題

使用者回報「有些實驗單純體驗會不知道意義」。量化後確認：任務導讀與實驗流程只有
8 種依模組族群產生的版本在服務 245 個實驗（同一模組內文字完全相同）、課本銜接以
模組為單位由 20 個實驗共用、237 個實驗的題目仍是同一組樣板，而且**沒有任何一個實驗
會告訴學生「達成什麼算成功」**。滑桿可以拉，但沒有目標也沒有觀察重點。

### 解法：讓模擬自己說出它的物理關係

關鍵觀察是模擬本身就知道答案。每個實驗都有滑桿（自變量）與讀數（應變量），
程式化掃描滑桿並記錄讀數，就能量出真正的關係，不需為 245 個實驗逐一撰寫。

- 以 log–log 迴歸求冪次律指數，r² > 0.995 才敢命名為「成正比／與平方成正比」等。
- 讀數改為同時保留未格式化的原始數值：先前讀四捨五入後的字串，會讓 P ∝ V² 被算成 V^1.93。
- 自變量改用「寫入後回讀的實際值」：滑桿有 step，寫入 475 會被吸附成 480。
- 每掃完一支滑桿就放回原位，否則下一支會在「前一支停在最大值」的狀態下量測。
- 步進器（stepper）與滑桿一併登記，否則只用步進器的實驗（拋體、雙狹縫、光電效應）完全探測不到。

### 避免說出錯誤的結論

探測時其他滑桿停在預設值，若模型在該工作點飽和（浮力實驗中物體已完全沒入，
沒入比例卡在 100%），掃描另一支滑桿會看到「完全沒有變化」而被誤判成物理無關。
因此每個「沒有變化」的組合都會在行程 35% 與 70% 兩個額外工作點複驗，
三個工作點都平坦才承認是不變性，否則降級為「範圍限制而非物理無關」並不出題。
此外會過濾讀數只是回顯滑桿數值的組合，並要求不變性的讀數對其他變因有明顯反應
（>25%），才值得指出「唯獨這一個沒有影響」。

### 建立在探測結果上的三個功能

1. **先預測再操作（POE）**：先問方向，再用真實數據揭曉並附冪次關係。
2. **可驗證的挑戰**：自動產生目標值（非單調時改為求極大值），即時判定並記錄過關。
3. **關係摘要**：列出實測到的關係，真正的不變性排在最前面。

### 驗證

- 對照已知物理 9 項全數通過：T ∝ √L、T ∝ 1/√g、等時性、T ∝ √m、T ∝ 1/√k、
  I ∝ 1/R、I ∝ V、P ∝ V²。
- 探測後滑桿必須完全還原（5 個實驗檢查），避免學生看到的不是設計好的初始狀態。
- 覆蓋率：探測成功 233/245（95%）、產生預測題 230（94%）、產生挑戰 225（92%）。
- 成本：平均每個實驗 4.9 ms（含不變性的兩次複驗）。
- 抽樣人工檢查生成文字：雙狹縫自動得出 Δx ∝ λ、∝ L、∝ 1/d；光電效應得出
  「最大動能與遏止電壓皆不受光強度影響」；拋體偵測到射程在中間角度出現極大值。

## 2026-07 採用障礙修正（依競品分析）

### 背景

競品分析確認專案定位為「開放教育資源，要讓老師與學生真的使用」。分析發現
產品功能已經領先（245 個對齊 108 課綱的物理模擬，PhET 全學科合計 121 個 HTML
模擬），但採用天花板接近於零，原因與功能無關。

### 發現

抓取線上網站後確認，搜尋引擎讀到的首頁字面上寫著「**0** 個實驗主題、**0** 個
互動模擬、搜尋結果共 **0** 筆」——所有內容都在解鎖後才由 JavaScript 生成，
245 個實驗的名稱一個都不在 HTML 裡。另外沒有 `sitemap.xml`、沒有 `robots.txt`，
也沒有任何一個實驗有可獨立索引的網址。

### 處置

密碼閘門是合作試用用途、事後會移除，因此不是拿掉而是抽成單一開關：

- 新增 `js/site-config.js`，`accessGate` 一個布林值同時控制密碼閘門與索引政策。
  開啟時自動注入 `noindex` 並讓 `robots.txt` 擋住爬蟲，避免在空殼狀態被收錄；
  關閉時閘門消失、`noindex` 不再產生、`robots.txt` 開放並附上 sitemap。
- 新增 `tools/build-static.js`：由課程資料產生 245 個實驗說明頁、245 份可列印
  學習單、總索引頁、sitemap 與 robots，全部含真正的 HTML 內容與結構化資料。
- 新增 `tools/verify-static.js` 並接進 GitHub Actions，確保「內容真的進得了 HTML」
  這件事不會再悄悄壞掉。
- 實驗頁加上學習單入口；首頁加上永續性宣告（開源、可自行架設、授權）與
  「模擬會自己說出物理關係」的差異化說明。

### 修掉的一個正確性問題

靜態頁不載入 MathJax，需要把 LaTeX 轉成純文字。初版用正規表示式處理
`\dfrac{}{}`，但分子常含巢狀大括號（`\dfrac{4\pi^{2}}{g}`）導致比對失敗，
括號被剝除後 **T² = 4π²L/g 會變成錯誤的 T² = 4π²gL**。改寫成會數括號深度的
解析器，並加上分式的括號保護規則（`((4π^2)/g)L` 而非會被誤讀的 `(4π^2)/gL`）。

### 驗證

- 靜態層檢查 12 項全數通過，包含「索引頁含有全部 245 個實驗標題」。
- 閘門開關兩種狀態各建置一次，確認 `robots.txt`、`noindex` 與應用行為三者一致。
- 公式轉換測試 8 項通過，重點檢查分母不會遺失。
- 全站回歸維持通過：245 個模擬 × 2 主題渲染、探測 25 項、工具層 27 項、
  隨機題 18 項、墨色層 15 項、邏輯 21 項、對比 AA 全數達標。

## 2026-07 手機可用性

### 稽核

寫了一支腳本解析 CSS，找出可互動元素在手機上的實際高度，對照 WCAG 2.5.5
建議的 44×44px。最嚴重的是 `input[type=range]`：軌道 5px、拇指 16px，
而滑桿是全站 241 個實驗共用、最常被操作的控制項。

另外量測實驗頁在 360px 寬手機上的堆疊高度：指令列 96 + 任務導讀 210 +
預測 190 + 挑戰 150 + 關係摘要 44 = **690px**，比一個 iPhone 的可視高度還高。
學生打開實驗看到的全是文字，得先捲過一整個螢幕才看得到模擬。

### 處置

- 觸控目標改用 `@media (pointer: coarse)` 判斷（問題來自手指粗細，不是視窗寬度），
  滑桿、時間控制列、工具列、指令列、膠囊、核取方塊、清單項目全部提升到 44px。
- 滑桿的做法：元素本身撐到 44px 高，軌道改用置中的背景圖繪製。
  沒有用「padding + 負 margin」的老招——`.sim-controls` 的 gap 只有 13px，
  扣掉上下各 17px 會變成負值，相鄰控制項會重疊。
- 實驗頁在 ≤760px 改用 flex order 重排：預測（壓縮後）→ 播放鍵 → 模擬台 →
  讀數 → 圖表 → 挑戰 → 關係摘要 → 導讀 → 指令列。畫布前的高度從 690px
  降到 202px，減少 71%，模擬在第一屏就看得到。
- 畫布工具的命中半徑在觸控裝置從 12px 放大到 24px，碼錶按鈕從 18px 加高到 30px，
  並在提示文字說明「工具啟用時畫布區域不會捲動」。
- 畫布高度上限改在 `sim-core` 的 `fit()` 裡處理（橫向手機限制為視窗高度的 68%），
  而不是用 CSS `max-height`——後者只會把畫好的內容壓扁。
- 補上瀏海手機的安全區留白、學習單表格的橫向捲動、以及點擊不反白與消除點擊延遲。

### 驗證

- 觸控目標稽核：54 條互動規則中僅剩 2 條低於 44px，且都是純顯示用的標籤
  （`.stepper-unit`、`.sim-command-stage`）。
- 觸控環境測試 6 項通過（以 `pointer: coarse` 模擬，確認提示文案與尺規供應邏輯）。
- 全站回歸維持通過：245 個模擬 × 2 主題渲染、工具層 27 項、探測 25 項、
  隨機題 18 項、墨色層 15 項、邏輯 21 項、靜態層 12 項、對比 AA 全數達標。

### 仍未驗證

所有檢查都是程式層面的。實機上的捲動手感、滑桿拖曳的靈敏度、以及工具在
小畫布上是否會擋住實驗內容，仍需要真的用手機開一次才能確認。

## 2026-07 效能與無障礙

### 效能：實驗程式改為延遲載入

量測發現首頁同步載入 **849 KB** 的 JavaScript，其中 **496 KB（58%）是 16 個實驗檔**——
但學生一次只會打開一個實驗，其餘 244 個的程式碼完全用不到，而且這些解析與執行
全部發生在畫面出現之前。這與前一輪的手機優化直接衝突。

- 新增 `tools/build-manifest.js` 產生「實驗 id → 檔案」對應表，`app.js` 在開啟
  實驗時才載入需要的那一個檔案，並在瀏覽器閒置時預先抓取相鄰實驗。
- 對應表**不是用正規表示式掃出來的**。regex 只找得到 104 / 245，因為有大量實驗
  是資料驅動、用迴圈註冊的，字串根本不在原始碼裡。改成「用假的 PhysicsLab 真的
  把每個檔案執行一次」，記錄它註冊了哪些 id——這個做法不受寫法影響，245 個全中。
- 加上 `openToken`，避免慢速網路下舊實驗的建置結果蓋掉使用者已經切換到的新實驗。
- `auditCurriculum()` 改為比對對應表，因為啟動時註冊表已經是空的。

結果：首次載入 **829 KB → 364 KB（減少 56%）**，gzip 後 120 KB；
開啟單一實驗最多再加 58 KB。

### 無障礙：模擬的文字描述

245 個實驗的內容全部畫在 canvas 上，對螢幕報讀器而言是空白方塊。使用報讀器的
學生只聽得到滑桿名稱，完全不知道畫面在演什麼、讀數多少、調整後發生什麼事。
PhET 有 89 個模擬提供 Interactive Description，本專案原本是 0。

新增 `js/sim-a11y.js`，利用探測引擎已知的滑桿、讀數與物理關係自動生成，
245 個實驗一次到位：

- 畫布加上 `role="img"` 與會隨狀態更新的 `aria-label`（實驗名稱、核心概念、
  目前參數、目前讀數、以及由模型實測到的關係）。
- 新增「文字版讀數」DOM 對照表，報讀器可逐項閱讀，一般使用者也方便抄錄。
- `aria-live` 播報：調整參數 350ms 後播報「改了什麼、讀數變成多少」，
  節流避免拖曳時洗版；動畫進行中只更新文字不播報，否則報讀器無法使用。
- 頁面層補上跳至主內容連結、側邊欄與實驗清單的地標名稱、修正標題階層
  （h2 後原本直接跳 h4）、裝飾元素標記 `aria-hidden`。

### 驗證

- 延遲載入測試 7 項：確認首頁不再同步載入任何實驗檔、啟動時註冊表為空、
  245 個實驗都能依對應表載入並成功建置。
- 無障礙測試 20 項：245 / 245 的畫布都有超過 40 字的描述、都有文字版讀數面板，
  頁面語意結構 7 項全數通過。
- 觸控目標稽核：56 條互動規則中僅剩 2 條低於 44px，皆為純顯示標籤。
- 其餘回歸全數維持通過。

### 仍未做到

PhET 的無障礙還包含聲音化（sonification）與觸覺回饋，以及針對每個模擬手寫的
情境描述。本專案的描述是自動生成的通用格式，資訊完整但不如手寫貼近情境。
另外所有驗證仍是程式層面的，尚未用真正的螢幕報讀器實測。

## 2026-07 快取策略與離線

### 發現的矛盾

實驗程式改成延遲載入後，首次載入從 829 KB 降到 364 KB。但 Service Worker
仍在 `install` 事件把 17 個實驗檔（485 KB）全部預先抓下來——等於剛省下的流量
又在背景花掉一次，對用行動網路的學生尤其不友善。安裝總負載達 960 KB。

### 處置

- `sw.js` 把實驗檔從 `VERSIONED` 抽成獨立的 `EXPERIMENTS` 清單，安裝時不再預抓；
  實驗檔改由既有的 stale-while-revalidate 在真正被開啟時快取。
  安裝負載 **960 KB → 475 KB**。
- 真正需要完整離線的人（例如老師要帶到沒有網路的教室），可以在首頁的
  「離線與安裝」卡片主動下載，過程有進度回報，單一檔案失敗不會中斷整體。
  要不要花這個流量，交給使用者決定。
- 加入 `beforeinstallprompt` 的安裝引導。按鈕預設隱藏，只有瀏覽器判定符合
  安裝條件時才出現——不做假的安裝提示。
- 離線時頂欄顯示明確提示，避免使用者以為網站壞掉。
- 預抓相鄰實驗時檢查 `saveData` 與 `effectiveType`，省流量模式或 2G 下不偷抓。

### 順手修掉的版面問題

首頁「學習中心」的格線原本寫死三欄，新增第四張卡片後會落到第二列且寬度對不齊。
改成 `auto-fit` 自動填滿。

### 驗證

離線與快取測試 17 項全數通過；其餘回歸維持通過。

## 2026-07 依 PhET 設計原則提升趣味性

### 先確認可套用範圍，不誇大

PhET《Look and Feel》「鼓勵探索」一節列出的技法，逐一量測本站的可套用範圍：

| 技法 | 可套用實驗數 | 判斷 |
| --- | --- | --- |
| 星球／情境切換（真實世界連結） | 3 | 太少，不值得做成系統 |
| 小謎題（little puzzles） | 57 有不變性、其中 24 個變因數足夠 | 值得做 |
| 極值探索 | 30 | 既有的挑戰系統已涵蓋 |

必須誠實記錄的一點：**PhET 的「有趣」主要來自每個模擬的客製美術與情境設計**
（溜冰場的溜冰者、發射南瓜的大砲、搓地毯觸電的角色）。那是逐一手工打造的，
PhET 24 年累積 121 個。沒有任何通用機制能替 245 個實驗變出等價的東西，
因此策略改為「通用機制提升整體 ＋ 單一實驗做到旗艦等級當範本」。

### 拋體運動旗艦改版

原本是「一條拋物線加兩個讀數」。改成一座可以瞄準的大砲：

- 四種發射物（保齡球 7 kg 到氣球 0.05 kg，質量差 140 倍）
- 可調砲台高度、可移動的靶，命中與否有明確回饋，落點會留下痕跡
- **可開啟空氣阻力**——這是整個改版的教學核心

物理改用數值積分（有阻力時沒有解析解），讀數直接取自模擬出來的路徑，
確保畫面與數字永遠一致。落地點用線性內插回到 y = 0，射程不受步長影響。

這一版能演示三件真空版本永遠看不到的事：
- 真空時四種發射物軌跡完全相同（伽利略），有阻力時保齡球 108.7 m、氣球 4.8 m
- 有阻力時最遠角度從 45° 降到 40°，且互餘角不再等射程
- 砲台架高到 15 m 時，最佳角度再降到 35°

### 「找出無關變因」小謎題

探測引擎已經知道哪個變因對某個讀數完全沒有影響，那正是一個有標準答案、
又必須真的動手試才答得出來的謎題，練的就是控制變因。
單擺問「哪一個參數不影響週期」（答案是振幅，即等時性），彈簧振子同理。

關鍵細節：**答案原本就寫在關係摘要裡**，因此解開之前那一行會被遮住，
否則學生展開摘要就直接看到答案，謎題失去意義。

門檻設為「至少三個變因」——只有兩個選項的話是丟銅板，不算謎題。
因此涵蓋 24 個實驗，而非全部 57 個有不變性的實驗。

### 驗證

- 拋體物理測試 15 項：真空射程與解析解誤差 < 0.15%、四種發射物射程完全相同、
  互餘角等射程、有阻力時最佳角度下移、架高後最佳角度再下移、讀數與公式一致。
- 謎題機制：24 個實驗產生謎題，答案在解開前確實被遮蔽。
- 其餘回歸全數維持通過；觸控目標仍為 58 條中僅 2 條純顯示標籤未達標。

## 2026-07 旗艦實驗第二批：能量守恆與電路

延續拋體運動的規格，再把兩個課綱核心主題做到同樣深度。

### 軌道上的力學能守恆（滑板場）

四種軌道形狀、可調質量與起始位置、摩擦係數、即時能量長條與總量線。
極端情況有合理反應：衝出軌道會停下並提示，摩擦把能量吃光後滑板者停住。

**修掉一個會毀掉整個教學目的的數值問題。** 初版用顯式積分推進速度，
但彎曲軌道的正確方程含向心項 −f'f''ẋ²/(1+f'²)，漏掉它會讓總能量持續漂移，
實測 6 秒掉 7.4%——畫面上那條「總量」線會自己往下滑，等於用一個不守恆的
模型演示守恆定律。

改用「積分定方向、能量定大小」：速度仍由切線加速度推進（否則從靜止放開時
K 恰為 0，位置永遠不前進，滑板者卡在原地），但每一步結束後用剩餘力學能
把速率投影回去。兩個問題同時解決，守恆變成結構上保證。

另外修掉一個因為重複綁定造成的 bug：`PL.ui.button` 已經綁了 click，
又額外 `addEventListener` 一個切換，兩個處理器互相抵銷（一個設 true、
另一個立刻 toggle 回 false），按鈕完全沒反應。

### 歐姆定律與電路

實體電路取代抽象滑桿：燈泡亮度隨功率變化、電子流速度正比於電流、
可切換單一／串聯／並聯。核心是 PhET 說的「以有意義的方式壞掉」——
電流超過額定時燈泡會燒斷，裝了保險絲則保險絲先斷。

**測試抓到一個讓整個設計失效的參數錯誤。** 初版保險絲額定設 2.5 A，
但燈泡額定電流是 √(12/3) = 2.0 A——保險絲永遠比燈泡晚斷，等於毫無保護作用，
「保險絲為什麼存在」這個教學目的完全不成立。改成 1.5 A 後才正確。

另外補上「換新之前先把電壓調低，否則會立刻再燒一次」的提示——
條件沒改就換新確實應該立刻再燒（那是合理的物理反應），但要講清楚才不會困惑。

### 驗證

- 能量守恆 10 項：6 秒總能量漂移 0.00%、三種質量速率完全相同、
  v = √(2gΔh) 與 K = ΔU 皆吻合、有摩擦時四項總和仍守恆。
- 電路 15 項：歐姆定律、串並聯總電阻、無保險絲時燒燈泡、
  有保險絲時保險絲先斷、降壓換新後恢復、正常範圍不誤觸發。
- 探測引擎測試同步更新：電路改版後燈泡的 3Ω 串在迴路裡，
  I 對 V 仍嚴格線性，對 R 則不再是純 −1 次方（這是更真實的電路）。

## 2026-07 旗艦實驗第三批：雙狹縫干涉

### 設計目標

這個實驗的價值不在條紋畫得漂亮，而在三件課本只寫一句話的事：

1. 一次只發射一顆光子，前 20 顆完全看不出規律，累積數百顆後條紋自己浮現——
   干涉不是光子之間互相作用，單一光子就會。
2. 「兩條單縫圖樣相加」不等於「雙縫圖樣」，差的那一項就是干涉。
3. 在縫上裝偵測器去看光子走哪一條，條紋立刻消失；關掉又回來。

實作上用拒絕採樣從理論強度分布抽出每顆光子的落點，
強度為單縫繞射包絡 sinc²(πa·sinθ/λ) 乘上雙縫干涉項 cos²(πd·sinθ/λ)；
裝上偵測器或選「兩單縫相加」時，干涉項替換為強度直接相加。

### 修掉一個會讓核心結論失效的量測錯誤

「條紋對比度」初版掃過螢幕 ±35% 的範圍取最大最小值。問題是**單縫繞射的
包絡本身就會由中央往外遞減**，因此即使完全沒有干涉，也會量到 0.465 的
假對比度——「打開偵測器條紋就消失」這個結論在數字上完全看不出來。

改成只在 ±Δx/2（一個條紋間距的一半）之內取樣。這個範圍內包絡幾乎是平的，
量到的才是干涉條紋本身。修正後：雙縫 1.000、偵測器開啟 0.026、
兩單縫相加 0.026，三者的差異一目了然。

### 驗證

- 雙狹縫測試 16 項：Δx = λL/d 在三組參數下與理論完全一致、
  波長越長條紋越疏、縫間距越大條紋越密、偵測器開關會正確切換干涉狀態、
  「兩單縫相加」沒有細條紋。
- 採樣正確性另外獨立驗證：以相同公式重寫一份強度函數，
  對 20 萬顆光子的落點做直方圖，與理論強度的相關係數 **r = 0.99971**，
  最高峰落在中央極大（0.26 mm，bin 寬度內）。

## 2026-07 旗艦實驗第四批：都卜勒、電磁感應、氣體動力論

七個旗艦實驗到此完成（拋體、能量守恆、電路、雙狹縫、都卜勒、電磁感應、氣體）。

### 都卜勒效應

救護車經過觀測者，波前一圈一圈以聲速擴散。速度上限刻意開到 480 m/s
（> 340 m/s 的聲速），推到極端時波前疊成馬赫錐，也就是音爆——
超音速時前方的觀測者在音源經過之前完全聽不到聲音。
頻率公式 f′ = f₀v/(v − vₛcosθ) 在三組參數下與理論完全吻合。

### 電磁感應

針對「磁鐵放在線圈裡就會發電」這個最常見的誤解設計：
磁鐵停在線圈正中央時磁通量最大，但電動勢是零（山頂的斜率為零）。
Φ 與 ε 兩條曲線同時顯示，讓「ε = −N dΦ/dt」用看的就懂。

修掉一個手動操作的體驗問題：拖曳滑桿時 input 事件不是每個影格都來，
逐格算出的瞬時速度會在「有動」與「沒動」之間跳，檢流計指針閃個不停。
改用指數平滑，拖曳時讀數穩定、放手後幾個影格內衰減到零——
「停住就沒有電動勢」這個結論仍然成立。

### 氣體動力論

**壓力不是用 PV = nRT 算出來再顯示的**，而是統計分子真的撞在活塞上的
動量變化（每次撞擊 2m|vₓ|）除以時間與受力面積。這是「壓力來自碰撞」
的字面實作。

修掉一個讓波以耳定律完全看不出來的取樣問題：初版每 0.25 秒取樣一次就
直接顯示，但 200 顆分子在該窗口內只撞到活塞約 25 次，卜瓦松雜訊讓讀數
在 12 到 100 之間亂跳，P×V 的誤差高達 86%。改成 0.5 秒窗口加上指數移動
平均（真實壓力計也是在做時間平均）後，等溫壓縮的 P×V 誤差降到 3%。

### 驗證

第三批 21 項全數通過，重點包括：
- 都卜勒頻率與理論一致、馬赫數正確、超音速判定正確
- 停在中央電動勢為零、推得越快電動勢越大、磁通量最大值與速度無關、
  電動勢正比於匝數 N（N=20 → 41.5，N=40 → 83.0，恰為兩倍）
- 等溫壓縮 P×V 守恆（誤差 3%）、v_rms 比值 1.73（理論 √3 = 1.732）

七個旗艦合計 77 項物理驗證，全站回歸維持通過。

## 2026-07 修正：深色主題下填色被誤翻成白色

### 這是實機截圖才發現的問題

使用者提供螢幕截圖後才看到：深色主題的畫面中間，兩個圖表區塊是**近白色的方塊**，
在整片深色版面裡非常刺眼。

原因在墨色層。`graph.frame()` 用 `sim-bg-1`（#070b11）填出圖表底板，
而墨色層有一條「深色系畫在深色背景上 → 換成亮墨」的規則——那條規則是為了
處理「深色的字寫在深色實驗台上會看不見」，卻同時套用到了填色面，
於是整塊底板被翻成 #eef3fa。

清點後發現實驗檔裡共有 **28 種深色填色**會被這樣誤翻，包括所有
`theme.shade()` 的襯底面。

### 修正

`ink()` 新增 `kind` 參數；填色（`kind === "fill"`）豁免「深色→亮墨」這條規則。
文字與線條維持原本行為，因為它們確實需要翻色才看得見。
淺色主題下「白色填色 → 深墨」的規則不受影響，那條是對的。

### 為什麼既有的測試抓不到

所有自動檢查都在驗「顏色與背景的對比是否足夠」。被翻成白色的底板**對比極高**，
每一項檢查都會通過——它不是對比問題，是「顏色語意」問題：
一塊本來要當背景的面，被當成前景元素處理了。
這類問題只有真的看到畫面才會發現。

已補上回歸測試（`ink-test.js` 新增 6 項），明確斷言深色填色在深色主題下
必須維持原樣、而深色文字仍須翻成亮墨。


---

## 第 N+1 輪：版面左右不等長

使用者截圖回報「排版也要注意一下，不要左右不同長度」。

### 找到三個問題

**1. 同一列的面板各自取自然高度**

`.content-grid` 與 `.sim-stage` 都寫了 `align-items: start`，
於是同一列的兩塊面板各自長到自己需要的高度：

| 位置 | 左 | 右 | 差距 |
|---|---|---|---|
| 外層卡片 | 互動模擬工作區 ≈ 1050px | 概念與題目 ≈ 1250px | 約 200px |
| 實驗台內 | 畫布面板 ≈ 445px | 實驗參數面板 ≈ 155px | 約 290px |

兩個框線收在不同的位置，看起來像破版。
改成 `align-items: stretch`，並把 `.card`、`.sim-visual-panel`、
`.sim-control-deck` 都設成直向 flex，讓撐出來的空白落在內容區裡面
（而不是變成卡片底部一條沒有背景的縫）；
畫布面板底部的識別列用 `margin-top: auto` 貼齊下緣。

**2. 學習單那一列完全沒有 CSS**

`app.js` 產生了 `.worksheet-link-row`、`.worksheet-link`、`.worksheet-link-note`
三個 class，但 `style.css` 裡一條規則都沒有。
結果渲染成「一條藍色底線的裸連結」直接黏著一段說明文字，中間沒有任何間距。
補上樣式：左邊一顆有邊框的列印按鈕，右邊一句灰色補充。

**3. 量測工具疊圖層偏移 12px**

`.sim-tool-host` 用 `position: absolute; inset: 0`，
但絕對定位是相對於**內距框**，而 `.sim-canvas-wrap` 有 12px 內距、
畫布本身從內容框開始——兩者差了一個內距。

這個 bug 不影響讀數（尺量的是疊圖層自己的像素，比例尺一致），
但把尺或量角器對齊畫面上的物體時會差 12px。
改由 JS 依 `cv.canvas.offsetLeft / offsetTop` 定位，各斷點的內距怎麼改都不會再錯位。

### 新增的測試：孤兒 class 掃描

第 2 點值得一般化。只要 JS 產生了某個 class 而 CSS 沒有對應規則，
畫面就會少一塊樣式，而既有的測試（對比、物理、無障礙）**全部不會有任何反應**——
和上一輪的白底板一樣，這不是「值錯了」，是「東西根本沒接上」。

`layout-test.js` 因此會掃描 `app.js`、`sim-core.js`、`sim-tools.js`、
`sim-insight.js`、`sim-a11y.js` 產生的每一個 class，
逐一確認 CSS 裡出現過。目前檢查 231 個 class，全部有樣式。

掃描順便抓到 `.checkpoint-choice-label` 也沒有樣式——
它在格線欄裡，遇到不含空白的長公式會撐破欄位、把 A/B/C/D 的圓圈擠掉，
已補上 `min-width: 0; overflow-wrap: anywhere`。

`observe` / `textbook` / `takeaway` 三個是刻意保留的語意修飾 class
（三段外觀一致，留給老師用自己的列印樣式單獨指定），已明列於允許清單。

### 這一輪測試本身也有 bug

`layout-test.js` 第一版回報了五個失敗，但那五個其實都是對的。
原因是解析器沒有先移除 CSS 註解——它把 `}` 之後到 `{` 之前的所有字元
當成選擇器，而我這輪新加的規則每一條上面都有註解，
於是 `.content-grid` 被讀成 `*/ .content-grid`，比對不到。

值得記一筆：**測試回報失敗時，先確認失敗的是程式還是測試。**

### 這一輪仍然沒有做的事

和前幾輪一樣，這裡驗的是「CSS 規則是否寫對」，不是「畫面上是否真的對齊」。
jsdom 沒有版面引擎，量不到任何一個實際像素。
上一輪的白底板與這一輪的三個問題，全部都是使用者截圖才發現的。
真正的版面驗收只有人打開瀏覽器看才算數。


---

## 第 N+2 輪：白塊的另一半，以及 58% 的實驗其實是空殼

### 一、上一輪的墨色層修正只做了一半

使用者截圖回報畫面上仍有很多突兀的白色。用「注入真實 CSS 變數值」的稽核重跑，
在**淺色主題**找到了完全對稱的同一個 bug：

```
#f3f7fc → rgba(22,32,46,1)     189 個實驗，最大面積 209440（整塊畫布）
```

`ink()` 有兩條方向相反的規則：

| 規則 | 條件 | 動作 | 上一輪 |
|---|---|---|---|
| 深色→亮墨 | 深色畫在深背景 | 翻成 #eef3fa | 已豁免填色 ✓ |
| 淺色→深墨 | 淺色畫在淺背景 | 翻成 #16202e | **忘了豁免** ✗ |

上一輪只修了第一條，於是深色主題的白方塊沒了，
淺色主題下每一張圖表底板卻被翻成近黑色。

**正確的判準不是「是不是填色」，而是「這個顏色是誰決定的」。**

`col()`、`theme.pale()`、`theme.shade()` 回傳的顏色**本身就已經隨主題變化**，
它們是刻意選好的表面色，墨色層不該再改一次。
墨色層存在的理由只有一個：搶救實驗檔裡**寫死**的顏色
（例如打點計時器紙帶上那 1667 個 `#e6edf3` 的點，在淺色主題下會白到看不見）。

因此改成登記制：主題衍生色在用作填色面時直接放行，寫死的顏色照舊搶救。
文字與線條不放行——`col("text")` 的深色字疊在深色物件上仍然需要墨色層介入。

順帶把 4 個共用的「畫面讀數牌」工具（涵蓋 141 個實驗）與 4 個襯底面板
從寫死的深色改成 `theme.shade()`，淺色主題下不再是白畫布上的黑方塊。

**結果：深色 0 筆、淺色 0 筆。**

### 二、稽核工具自己壞了四次

這一輪最花時間的不是修產品，是修測試。依序踩到：

| 版本 | 症狀 | 原因 |
|---|---|---|
| v1 | 什麼都測不到 | jsdom 沒有樣式引擎，`col()` 一律回傳程式裡的深色 fallback |
| v2 | 淺色主題回報 29795 筆假黑洞 | 用 `indexOf("@media print")` 之前的內容當解析範圍，但 print 區塊出現在淺色主題**之前**，整組淺色變數被砍掉 |
| v3 | 把青綠 `#34d3c4`、橘 `#ffab80` 算成白塊 | 用「亮度 > 0.4」判定，但刺眼的白塊是**灰白**不是亮色，要同時看彩度 |
| v4 | 注入完全沒生效 | 只改了 `window.getComputedStyle`，harness 在載入時已把它複製成 `global.getComputedStyle` 這個獨立參照，而 sim-core 用的是後者 |

現在每個主題開跑前會先斷言 `col("sim-bg-1")` 真的讀到該主題的值，
讀不到就直接中止並印出「以下結果不可信」。

**教訓：一個回報 0 筆問題的稽核，和一個根本沒在測的稽核，輸出長得一模一樣。**
測試必須先證明自己有在測。

### 三、58% 的實驗是「兩根滑桿 + 一條裝飾曲線」

使用者說「很多實驗看不懂在教什麼」。清點後發現：

| 類型 | 數量 |
|---|---|
| 專屬實作（拋體、能量軌道、電路…） | 103 |
| **泛用模板產生** | **142** |

模板只有三個函式：`calc(a,b)` 給讀數、`chart(x,b)` 給曲線、`status()` 給一句話。
畫面上的圖大多不隨參數改變，第二根滑桿完全看不出作用。

#### regression-lab：每一個數字都是編的

這是使用者截圖的那一個。σ 設成 0、讀數顯示「殘差 RMS = 0」，
畫面上的點卻明顯偏離直線。查下去發現：

```js
const ideal = y0 - (i/9)*(h-30)*0.8;   // 固定斜率，完全不理會「真實斜率 k」
const n = Math.sin(i*7.3) * a * 8;     // 「雜訊」是確定性函數，不是隨機量
D.line(ctx, x0+18, y0-12, x0+w-18, 48, c, 2.2);  // 「擬合線」是兩個寫死的端點
```
```js
殘差 RMS  = σ × 0.72      // 憑空乘一個係數
擬合斜率 k̂ = k + σ × 0.12  // 真實的最小平方估計是無偏的，不會隨 σ 系統性偏高
```

已改寫成真的做最小平方：資料由 `y = kx + b + 高斯雜訊` 產生，
斜率截距用正規方程解出，殘差是資料點到擬合線的實際垂直距離，並畫出殘差線與殘差圖。

新增「彎曲 c」滑桿：資料看起來仍像直線，但殘差圖會出現 U 形——
**殘差一旦有規律，就代表直線這個模型選錯了**。
這正是課綱那條「隨機雜訊與系統誤差不同」要教的事。

驗證用統計性質而不是「有沒有畫出東西」（會畫圖的假實驗正是上一版）：

```
σ=0        → RMS = 3.2e-15，k̂ 完全等於 k，R² = 1
RMS/σ      → 0.89, 0.89, 0.89, 0.89（四個 σ 值，比例固定）
80 次重新取樣 → 平均偏差 −0.0004，散布 0.0857
理論標準誤 σ/√Σ(x−x̄)² = 1/√143 = 0.0836
```

實測散布 0.0857 對上理論 0.0836，誤差 2.5%。這是真的統計。

### 四、模板改造

**1. 曲線一律由 `calc()` 導出。**
原本另傳一個 `chart()` 重寫同一條公式，兩邊可能算出不同答案。
改成單一真相來源後，「亮點浮在曲線外」這類錯誤在結構上不可能發生。

**2. 同時畫出第二個參數在兩端的曲線（淡虛線）。**
學生看得到「調大它會把整條關係往上／往下搬」，而不是一條孤零零的線。

**3. 關係圖說明自動判讀曲線形狀並寫成中文。**
例如：「「電壓 V」變大時，「電流 I」跟著變大，形狀接近直線。
虛線是「電阻 R」拉到兩端時的同一條關係——把它調大會讓整條曲線往下搬。」
取代原本 142 個實驗共用的「曲線固定第二項參數；亮點顯示目前操作條件」。

**4. 修好 6 張畫在錯變數上的關係圖。**
`ΔA/A = 2ΔL/L` 與邊長無關、LED 波長只由能隙決定、聲級只由振幅決定——
輸出不隨第一根滑桿變化，圖就是一條水平線。
scene 依位置使用 a、b，所以不對調滑桿，改讓關係圖知道該掃哪一根。

其中 `electrostatic-shield`（法拉第籠殼內場強恆為零）的水平線**是正確的**，
已列入允許清單並註明理由——那條線就是結論。
`rgb-color-mixing` 的自創亮度公式改成真實的相對亮度
`Y = 0.2126R + 0.7152G + 0.0722B`（順帶教到綠光對感知亮度的權重最大）。

**5. 控制面板改印課綱本來就寫好的內容。**
`concept` 與 `points` 一直都在課程地圖裡，模板卻從來沒用過，
印的是「調整參數後，同步比較裝置中的現象、量測讀數與下方關係圖」——142 個實驗全都一樣。
現在每個實驗會顯示「這在教什麼 / 怎麼操作 / 該看出來的重點」，且都用自己的變數名稱。

### 五、這一輪仍然沒有做的事

模板實驗的**畫面**仍然大多是裝飾性的：讀數、關係圖、說明都對了，
但畫布上的裝置本身多數不會隨參數動起來。要做到像拋體或能量軌道那樣，
必須逐一改寫，不是模板層面能解決的。

另外一如既往：所有檢查都在 jsdom 裡跑，沒有任何一個實際像素被驗證過。


---

## 第 N+3 輪：把「畫面有沒有在表達物理」變成可量測的東西

使用者要求：「不要讓學生看起來像是在看不知名動畫」「不要出現一些球在旁邊
不知道意義為何的動」。

### 判準

對每個實驗錄下所有繪圖呼叫的座標，把同一根滑桿設到最小／中間／最大各跑 2 秒，
比較三次的繪圖軌跡。**如果圖形完全一樣，那根滑桿對學生就是沒有作用的**
——不管讀數上的數字變了多少。

### 結果

| | 初次盤點 | 修正後 |
|---|---|---|
| 每根滑桿都會改變畫面 | 207 | **222** |
| 部分滑桿是擺設 | 31 | 22 |
| 拉任何滑桿畫面都不變 | **6** | **0** |

### 最常見的病因：座標軸把效果 normalize 掉了

這是最值得記住的一條。`shm-graph` 的座標軸原本寫成：

```js
{ x0: 0, x1: 2 * T, y0: -A * w * w * 1.1, y1: A * w * w * 1.1 }
```

兩軸都跟著參數自動縮放，於是波形**永遠佔滿整個框、永遠顯示剛好兩個週期**。
學生把振幅從 0.5 拉到 2、把週期從 1 拉到 5，畫面一動也不動。
三根滑桿有兩根等於白放。

同樣的錯出現在 `energy-track`（長條除以當下總能量 → 質量加倍看不出來）
與 `distance-displacement`（位置除以去程距離 → 4 m 和 30 m 畫起來一樣長）。

**把座標軸縮放成剛好貼合資料，等於親手把滑桿想示範的效果消掉。**
改用固定比例尺之後，三個實驗的滑桿全部恢復作用。

### 第二種病因：參數只印成文字，畫出來的東西尺寸寫死

- `resistors`：電阻方塊永遠 56px 寬，R₁、R₂、V 只改方塊裡的數字
- `ballistic-pendulum`：木塊永遠 44×40、子彈永遠半徑 5，且只在飛行中才畫
- `newton3`：車子永遠 44px 寬，力箭頭只在「互推中」才出現
- `potential-e`：上下極板固定畫在 y=40 與 H−40，「板間距」完全不影響畫面
- `heat`：溫度只改變方塊顏色——顏色沒有刻度，學生無法從畫面判讀溫度

修法一律是讓畫出來的東西直接編碼參數：

| 實驗 | 現在畫面上會變的東西 |
|---|---|
| resistors | 方塊長度∝R、鋸齒數∝R、電流點的密度與速度∝I、電池高度∝V |
| newton3 | 車寬∝質量、成對的作用力箭頭恆常顯示、兩支長度不同的加速度箭頭 |
| ballistic-pendulum | 木塊尺寸∝M、子彈半徑∝m、待發射時先畫出「預期擺升高度」虛線 |
| inertia | 靜止待推時就畫出初速與摩擦力箭頭，並標出預計停止位置 |
| conservative | 三條能量長條：重力功不隨路徑變、摩擦功隨路徑長變 |
| heat | 兩支有刻度的溫度計＋終溫虛線（虛線偏向質量大的一邊） |
| potential-e | 板間距真的隨 d 改變，電場箭頭密度∝E=V/d |
| energy-track | 能量長條改用固定參考能量，質量大就是明顯比較高 |

### 稽核工具自己錯了六次

這一輪又是修測試比修產品花時間。全部記在這裡：

| 版本 | 誤判 | 原因 |
|---|---|---|
| v1 | beats、superposition 被判成靜態插圖 | 只 hook `PL.draw.*`，這些實驗用原生 `ctx.lineTo()` 畫波形 |
| v2 | 一堆實驗顯示 3~5% 有反應 | 文字與圖形混在一起算，變的其實只有標籤上的數字 |
| v3 | energy-track 等旗艦被判成 0% | 只在 t=0 量，但全站「進場不自動播放」，按下播放前本來就靜止 |
| v4 | distance-displacement 全部 0% | 該實驗的迴圈在未播放時直接 `return`，兩組錄影都是空的 |
| v5 | coulomb 被判成兩根滑桿失效 | 雜湊用**加總**，順序無關；兩支力箭頭互換方向後湊出同一個值 |
| v6 | shm-graph 相位、ampere-force 夾角被誤報 | 只比 min 與 max，但相位 0° 與 360°、夾角 0° 與 180° 是同一個狀態 |

v5 特別值得記：**加總型雜湊對「對調」是盲的**，而物理模擬裡「對調」正是
最常見的變化之一（作用力反向、電荷變號、左右互換）。

### 剩下的 22 個

清單在上面的稽核輸出裡。這些都只有一根滑桿沒有視覺作用，
多數是「該參數只在模擬跑起來之後才有效果」（例如摩擦係數要等物體開始滑）
或「該參數只影響讀數不影響幾何」（例如衛星質量不影響軌道形狀——
這其實是正確的物理，但畫面上應該要有別的方式讓學生看到這件事）。
需要逐一判斷是 bug 還是本來就該如此，還沒做完。


---

## 第 N+4 輪：把剩下的 22 個做完

### 結果

| | 開始時 | 現在 |
|---|---|---|
| 每根滑桿都會改變畫面 | 207 | **244（全部）** |
| 部分滑桿是擺設 | 31 | **0** |
| 拉任何滑桿畫面都不變 | 6 | **0** |

244 個有滑桿的實驗，每一根滑桿都會讓畫面產生可見的改變。

### 修法分成三類

**一、座標軸把效果 normalize 掉（最常見）**

| 實驗 | 原本 | 改成 |
|---|---|---|
| shm-energy | 位置比例尺 `(W−120)/(2A)`、能量圖 y 軸 `E×1.1` | 固定比例尺（振幅上限 0.5 m、勁度上限 60） |
| resonance-tube | 管子永遠畫滿整個畫面寬 | 管長以上限 1 m 對應最大寬度 |
| rope-over-edge | `length` 讀出來卻沒用在任何幾何上 | 繩子實際長度隨 L，桌面段與垂落段再依比例分配 |
| error-propagation | 正方形比例尺以 `maxL` 為分母 | 固定以邊長上限 5.6 m 為準 |

**二、參數只影響公式，物件畫得一樣**

| 實驗 | 現在畫面上會變的東西 |
|---|---|
| centripetal | 球的半徑∝質量，向心力箭頭長度∝實際的 mω²r |
| induction | 磁鐵尺寸與周圍磁力線圈數∝磁鐵強度 |
| standing-wave | 抖動頻率 f = nv/2L，波速快弦就抖得快 |
| semiconductor-led | 電壓低於能隙時 LED 是暗的，跨過門檻才亮、越亮光暈越大 |
| exoplanet-transit | 行星掃過恆星的時間∝軌道週期 |
| sound-properties | 波前間距∝1/f，振幅決定波前粗細與喇叭大小 |
| satellite-energy | 衛星本身變大，但軌道與速度紋風不動（並明講這就是結論） |
| critical-angle | 真的以 θ 入射，未達臨界角就折射出去，超過就只剩全反射線 |
| electrostatic-shield | 金屬殼有真實厚度，箭頭止於殼外、殼內空白 |

**三、參數要等模擬跑起來才有效果**

μₖ（動摩擦）、爆炸釋放、擺動次數這類參數在按下按鈕之前本來就沒有作用。
物理上沒錯，但學生會以為滑桿壞了，也學不到那個概念。改成事前就呈現：

- `wall-friction` / `incline-applied-force`：並排的「最大靜摩擦 μₛN」與「動摩擦 μₖN」對照棒，一眼看出滑動後摩擦會變小
- `recoil`：發射前先畫出預期的兩支速度箭頭，並標出 M·v₁ = m·v₂
- `pendulum-measure-g`：把 n 畫成一排格子，已完成的填滿，看得到「還要再擺幾次」
- `energy-track`：軌道本身的粗細與顏色隨 μ 改變（μ=0 是光滑細線）
- `quantum-transitions`：10 次機會中發生幾次，用實心箭頭與短截線表示，而不是只改顏色深淺

### 一條原則：不要只靠顏色傳達數量

`heat` 用方塊顏色表示溫度、`quantum-transitions` 用箭頭深淺表示機率——
兩者都被量化稽核判為「畫面沒有變化」，而這個判定其實是對的：
**顏色沒有刻度，學生無法從顏色讀出數值，色弱使用者更是完全分不出來。**
兩處都改成有幾何量的呈現（溫度計柱高、箭頭有無）。

### 稽核工具的第七個坑

只錄座標、沒錄線寬。`wire-resistivity` 用 `ctx.lineWidth` 表示導線粗細，
這對學生是很清楚的訊號，錄影機卻看不到，於是被誤判成滑桿無作用。
補上在 `stroke()` 時記錄當下線寬之後才正確。

### 我自己犯的兩個錯（都被稽核抓到）

1. `wall-friction` 的補丁用了 `q.N`，但那個模型裡正向力叫 `q.F`——
   算出 NaN，長條根本沒畫出來。稽核回報「還是 0%」才發現。
2. 依據過期的行號，把 `rope-over-edge` 的補丁打進了隔壁的 `table-hanger`，
   讓一個本來完全正常的實驗直接崩潰。已還原，並改用 `PL.register("id"` 定位。

**修東西的時候，最容易弄壞的是本來就好好的那個。**

### 仍然沒有做的事

一如既往：所有檢查都在 jsdom 裡跑，量的是「繪圖指令有沒有不同」，
不是「畫面看起來對不對」。指令不同不代表學生看得懂，
也不代表版面沒有重疊、顏色沒有打架。這部分只有人打開瀏覽器看才算數。


---

## 第 N+5 輪：引擎層教學元件、三個旗艦、測試進版控

### 一、測試曾經全部消失

上一輪的二十幾支測試寫在 `/tmp`，暫存目錄被清空後一次全沒了。
**測試放在會被清空的地方等於沒有測試。**
已全部重寫並移進 `tools/tests/`（15 支 + harness + 共用函式庫 + 執行器），跟著程式進版控。

```
node tools/tests/run-all.js          # 全部
node tools/tests/run-all.js --fast   # 略過兩支耗時稽核
```

### 二、七個引擎層教學元件

對照市面上做得最細的中學物理模擬後，把差距歸納成七類通用元件，
做進 `PL.ui.*`，任何實驗都能用：

| 元件 | 解決的問題 |
|---|---|
| `verdict` | 結果先講，不要讓學生自己猜「這樣算成功嗎」 |
| `derived` | 攤開中間量——學生卡住的通常不是最後一步 |
| `presets` | 一鍵跳到「剛好臨界」這種關鍵點 |
| `chartTabs` | 同一組資料的多種看法（x-t／v-t／Δx-t） |
| `magnifier` | 有些量整體看根本看不到 |
| `causality` | 「誰決定誰」——學生錯的常是因果方向不是算式 |
| `procedure` | 編號步驟＋鐵律警示框 |

### 三、三個旗艦

**長度測量與量具讀數**（全新主題，原本完全沒有）
放大窗的理由很硬：50 分度游標一格 0.02 mm，整支尺畫出來時 1 mm 約 8px，
**0.02 mm 連半個像素都不到**。真卡尺也要湊近瞇眼，模擬本來就該給這扇窗。
讀數規則隨機 200 個位置驗證自洽；練習模式的誘答刻意做成學生真會犯的錯
（多寫一位、少一格、漏看半毫米線），答錯給對應的專屬回饋。

**追及與相遇**（全新）
處理了本單元最經典的錯誤：**剎停的車不可以倒退**。
直接套 x = v₀t + ½at² 會讓剎停的車往回走，追上時刻整個算錯。
測試逐條驗證：等速追等速的追上時刻、等加速追等速的最大間距 44 m（對照解析解）、
「速度相等是間距極值」在極值前後各兩點都成立。

**力學能守恆與圓環軌道**（全新）
h ≥ 2.5R 學生都背得出來，但問「為什麼不是 2R」就答不上來。
把 h 設成剛好 2R 可以親眼看到小球在環頂之前脫軌——
依 PhET 原則，模擬要以「有意義的方式壞掉」。

### 四、測試抓到的四個真問題

**1. 我自己的新實驗有刺眼白塊。** 卡尺本體寫死鋼灰，在深色畫布上是 20400 px² 的大片淺灰。
改成隨主題的金屬色。

**2. `.sim-verdict-text` 沒有樣式。** 孤兒 class 掃描抓到——加了元件卻漏了 CSS。

**3. 效能測試自己量錯指標。** 回報首次載入 515 KB 超標，但那是原始位元組；
GitHub Pages 傳輸會 gzip，實際是 176 KB。門檻改設在壓縮後。
**量錯指標的測試比沒有測試更糟**——它會逼人去優化一個不存在的問題。

**4. 兩個新旗艦每影格重算全部幾何。**
`loop-track` 畫一次軌道呼叫 300 次 `pointAt()`，每次內含 120 步線性搜尋；
`chase` 每次更新掃描 2400 點三遍。桌機看不太出來，手機會明顯掉格。
改成弧長對照表 + 二分搜尋、分析結果以參數為鍵快取。
現在最慢的 `loop-track` 是 3.0 ms／影格（預算 20 ms）。
並新增「每影格預算」測試，248 個實驗全部在 8 ms 以內。

### 五、臨界值要用容差判斷

h 剛好等於 2.5R 時，mg(h−2.5R) 在浮點下算出 −2.22e-16，
用 `margin >= 0` 會掉到「過不去」那一邊——明明是課本的標準答案。
加了容差，並讓「剛好臨界」有自己的判定（環頂速率恰好 √(gR)、正向力恰好為零），
這本來就是這一題最值得標出來的狀態。

### 六、稽核工具的第八個坑

`meaning-audit` 加入新實驗後從 21 秒暴增到數分鐘：
六萬多影格，而絕大多數滑桿在前幾影格就看得出差別，跑滿純粹是浪費。
改成三個實例並行步進、一發現差異就收工——35 秒，且結果不變。
它也立刻抓到 `loop-track` 的質量與重力對畫面沒作用
（物理上臨界高度確實與兩者無關，但學生還是該看到東西：
球徑改為隨質量、底部速度箭頭隨 g 改變）。

### 仍然沒有做的事

- 交流發電機與變壓器的因果面板還沒做
- 其餘 240 幾個實驗仍是幾何線條，沒有旗艦等級的器材插畫
- 一如既往：所有檢查都在 DOM 替身裡跑，量的是繪圖指令不是畫面


---

## 第 N+6 輪：第四個旗艦（變壓器與交流發電機）

### 變壓器：把「誰決定誰」做成核心

原本的版本只有三根滑桿與一個電壓讀數——沒有負載、沒有電流、沒有功率，
等於只把 U₁/U₂ = n₁/n₂ 畫成圖。學生背得出這條比例式，
但問「把負載換成更耗電的，原線圈電流會怎麼變」就答不出來，
因為課本只給比例式，沒講因果方向，而這一題的因果是**分岔的**：

| 量 | 方向 | 意思 |
|---|---|---|
| 電壓 | 原 → 副 | U₂ 由 U₁ 與匝數比決定，副邊接什麼負載都不影響 |
| 電流 | 副 → 原 | I₂ 由負載決定，再回頭決定 I₁（最常被搞反的一條） |
| 功率 | 輸出 → 輸入 | 負載越重，輸入功率越大 |

三個量三個方向，這正是因果面板要講的事。測試也照這個驗：
換負載時 U₂ 完全不變，而 I₁ 跟著變成四倍。

另外補上兩個課本強調、模擬卻很少做的狀態：

- **直流輸入** → 磁通量不變就沒有感應，副邊完全沒有輸出（畫面上磁通箭頭變灰、靜止）
- **空載** → 有電壓但沒有電流，斷口直接畫出來

### 交流發電機：中性面才是重點

學生會背 e = nBAω·sin(ωt)，但問「轉到中性面時電動勢多少」常常答「最大」——
因為那時磁通量最大。這正好答反了：**極值處的變化率是零**。

所以把「過中性面：e = 0，電流換向」做成會亮起來的判定，
而不是埋在波形裡讓學生自己看。Φ 與 e 畫在同一張圖上，
兩條線錯開四分之一週期——Φ 的山頂正好對上 e 的零點。

轉角改成**從中性面起算**（θ = ωt），這樣 Φ = BA·cos θ、e = nBAω·sin θ，
「θ = 0 時 Φ 最大而 e 為零」直接寫在式子裡。

四個疊加層開關：磁感線（密度隨 B，這是 B 唯一看得見的地方）、
中性面、法線 n̂、電流流動（方向隨 e 的正負反轉）。

### 目前狀態

- **248 個實驗**，每一根滑桿都會讓畫面產生看得見的變化（量化驗證）
- **16 支測試**，全部在 `tools/tests/`，跟程式一起進版控
- 深色與淺色主題都沒有刺眼白塊或黑洞
- 每影格更新最慢 3.0 ms（預算 20 ms）

### 仍然沒有做的事

四個旗艦（量具讀數、追及相遇、圓環軌道、變壓器與發電機）有了截圖等級的
教學結構，但**其餘 240 幾個實驗仍是幾何線條**，沒有木質軌道、金屬卡尺、
三維發電機那種器材插畫。那是一個一個畫的工作，不是引擎層能解決的。

一如既往：所有檢查都在 DOM 替身裡跑，量的是繪圖指令不是畫面。


---

## 第 N+7 輪：不是每個實驗都要插畫，是每個實驗要有它自己該有的設計

上一輪我把「還沒做完的事」寫成「其餘 240 幾個實驗沒有器材插畫」。
**這個框架是錯的。** 木質軌道、金屬卡尺只有那幾個實驗用得上，
真正該做到全站的是：**這個實驗的內容需要什麼教學鷹架，它就要有什麼。**

### 把「該有什麼」變成可盤點的

內容類型由課綱條目（concept 與 points）判定——課綱怎麼寫，
就代表這個實驗要教什麼：

| 內容出現 | 就需要 | 理由 |
|---|---|---|
| 至少／才能／否則／臨界 | 判定徽章 | 學生要知道現在這組設定是成功還是失敗 |
| 決定／方向／與…無關 | 因果面板 | 學生錯的常是方向而不是算式 |
| 先…再…／作圖求／讀到 | 步驟教學 | 這類題目失分在流程不在概念 |
| 兩個以上的中間量 | 衍生量卡 | 把解題過程攤開 |
| 剛好／恰好／等於…時 | 情境預設 | 一鍵跳到那個關鍵點 |

初次盤點：248 個實驗，**125 個有缺口**，其中因果面板缺 88 個。

### 用實測結果自動生成，不是套模板填空

探測引擎本來就會掃描每根滑桿、量出它與讀數的關係（T ∝ √L 這類）。
這些是**從模擬自己的行為量出來的**，因此每一句都具體且為真：

> 誰決定「週期 T」（由模擬實測）
> · 重力 g　1.60 → 20　得 週期 T 7.025 → 1.987
>   影響最大：把「重力 g」調大，「週期 T」反而變小（與平方根成反比）。
> · 初始角 θ₀　全範圍　週期 T 固定為 2.838
>   完全不影響：不管怎麼調，「週期 T」都不變。「沒有關係」本身就是結論。

情境預設同樣自動生成，其中一項特別重要：**「驗證 X 無關」**——
把不影響的變因推到上限，讓學生自己看著讀數紋風不動。
不變性是最容易被當成「實驗做壞了」的結果，親手驗證比讀一句說明有效得多。

實驗自己已經寫了因果面板或預設就不會再加。手寫的那幾個
（變壓器的「誰決定誰」、圓環的「兩個條件要分開看」）講的是自動探測看不出來的東西——
例如電流的因果是副邊決定原邊，這不是量滑桿量得出來的。

**結果：缺口 125 → 57。因果面板 88 → 14，情境預設 19 → 2。**

剩下的 57 個（判定徽章 26、步驟教學 16、因果面板 14、衍生量卡 7）
需要逐題的專業判斷，自動生成只會產出空話。清單可用
`node tools/tests/design-audit.js` 隨時重新產生。

### 這一輪修掉的三個測試錯誤

**1. 氣體實驗的測試在讀瞬時值。**
壓力是統計量（由分子實際碰撞算出），本來就會漲落。
測試只讀某一瞬間的 P×V，量到的大半是雜訊，離散度在 11%～23% 之間隨機跳，
**時而通過時而失敗**。先穩定再對讀數取時間平均之後，離散度是 0.3%～2%——
物理其實非常準，是測試量錯了。

**不穩定的測試比沒有測試更糟：它會讓人開始忽略紅燈。**

**2. 掛鉤累積導致記憶體耗盡。**
`meaning-audit` 在每次建置時都註冊一個 `onBuilt` 掛鉤，
全站掃完累積五千多個，每一個都抓著一整棵 DOM 樹不放——
既是 O(n²) 也是記憶體洩漏。加入自動生成的教學元件、每棵樹變大之後，
直接把 Node 撐爆（FatalProcessOutOfMemory）。
引擎的 onBuilt 沒有提供取消註冊，所以測試這端必須只註冊一次。
`em-test` 與 `physics-test` 有同樣的模式（呼叫次數少所以還沒爆），一併修掉。

**3. 孤兒 class 掃描抓到 `.sim-insight-block` 沒有樣式。**
又是「加了元件卻漏了 CSS」——這支掃描已經抓到第三次了，很值得留著。

### 目前狀態

- 248 個實驗，**每根滑桿都會讓畫面產生看得見的變化**（實測，非宣稱）
- 191 個實驗的教學鷹架符合其內容需求，57 個有缺口且清單可重現
- 16 支測試全過，深淺主題皆無白塊黑洞，每影格最慢 3.0 ms


---

## 第 N+8 輪：57 個缺口補完

### 兩個需要教師判斷的決定

我把兩件不該自己猜的事問了出來，答案決定了整批內容的寫法：

**一、學生必做實驗的步驟寫哪一種？** → **實體實驗室的標準流程**

不是「在這個畫面上怎麼拉滑桿」，而是「拿到器材之後怎麼做」：
量幾次、怎麼記錄、作什麼圖、從斜率求什麼。
模擬因此成為實驗課的**預習工具**——學生進實驗室前已經知道要做什麼。

**二、每個實驗要不要納入誤差來源？** → **要，這才是實驗課的重點**

於是 16 個學生實驗每一個都附「鐵律」框，寫最常見的操作錯誤：

| 實驗 | 鐵律 |
|---|---|
| 單擺測 g | 擺長要量到**球心**不是球頂；計時數多次再除，攤掉按碼錶的反應時間 |
| 伏安法測電阻 | **大電阻用安培計內接、小電阻用外接**。接反了圖形照樣是漂亮直線，看不出來 |
| 惠斯登電橋 | 準確度來自「**指零**」不是讀數值——指零時檢流計自身的刻度誤差完全不影響結果 |
| 共鳴管測聲速 | 不要用 L₁ = λ/4 直接算，管口有**管口修正**；用**兩次共鳴的差**可以完全消掉它 |
| 位移法測焦距 | 好處是**不必量物距與像距**——透鏡中心很難準確定位，這個方法完全繞開它 |
| 電池電動勢 | 電池會發熱、內阻會變，整個實驗要**盡快完成**且每組之間讓它休息 |
| 電阻與溫度 | 加熱太快時**水溫上去了線圈還沒跟上**，量到的是兩個不同溫度的東西 |
| 電流天平 | 有效長度只算**位於磁場中**的那一段，且導線必須與磁場垂直 |
| 凸透鏡成像 | **虛像接不到光屏上**。u < f 不是實驗失敗，要移開光屏用眼睛看 |
| 面鏡成像 | 算出**負的像距不是算錯**，正是「這是虛像」的意思 |

**三、方法類實驗的判定怎麼辦？** → **改成自我檢核式**

單位換算、實驗設計、誤差傳遞這類沒有自然的「成功／失敗」，
硬給判定就是假裝有標準答案。改成提示該檢查什麼：

> 自我檢核：面積的相對不確定度是邊長的 2 倍。
> 平方關係會放大不確定度——結果要同時寫出數值與範圍。

### 內容集中在一個檔案

57 個缺口的內容全部寫在 `js/teaching-notes.js`，不去改 40 幾個實驗檔。
理由：分散寫進各檔就沒有人能一眼看完，也很難維持一致的用語。
集中之後，這個檔案等於「這個站在教什麼」的可審閱文件。

| 類型 | 數量 |
|---|---|
| 判定徽章 | 26（20 個物理判定 + 6 個自我檢核） |
| 實體實驗室流程 + 誤差鐵律 | 16 |
| 因果面板（自動探測看不出來的） | 17 |
| 衍生量卡 | 7 |
| 情境預設 | 2 |

手寫的因果面板專講探測量不出來的東西，例如：

- **彈道擺**：兩個階段要分開算——碰撞是動量守恆但動能不守恆，
  擺升才是力學能守恆。合起來用能量守恆一次算完是最常見的錯。
- **楞次定律**：反抗的是**變化**不是磁場本身；而且「為什麼一定是反抗」
  的答案是能量守恆——若感應電流助長變化，磁鐵會自己越跑越快。
- **拉塞福散射**：重點不是「大部分粒子直線通過」，而是**極少數被反彈回來**。

### 新增的測試防的是「空話」

`teaching-notes-test.js` 檢查的不是「有沒有出現」，而是內容有沒有提到
**這個實驗自己的物理量或器材**。一句「請注意實驗安全」放在每個實驗上都成立，
因此也等於什麼都沒說。64 項全過。

測試本身也修了一個誤判：第一版只認手寫的五種元件，
把 `orbit`、`dispersion` 這些其實已經有自動關係摘要的實驗報成「沒有鷹架」。

### 目前狀態

- **248 個實驗，教學設計缺口 0**（`node tools/tests/design-audit.js` 可重現）
- 每個實驗都至少有一種教學鷹架，且內容指向它自己的物理
- 17 支測試全過；深淺主題無白塊黑洞；每根滑桿都改變畫面；每影格最慢 3.0 ms
